---
type: Guard Layer
title: L3 Symbol Exclusion
description: Blocks BUYs on symbols in the data-driven L3 exclusion list.
lilith_safe: false
tags: [guard, l3, symbol-exclusion, optuna]
layer: L3
on_fail: block
---

# Purpose

Enforces a symbol exclusion list **in code** (not just in the prompt). The list
is data-driven from the `L3_EXCLUDED_SYMBOLS` row in
[optuna-params](/system/echidna-tables/optuna-params.md). A BUY on an excluded
symbol is rejected (`blocked_by` via `logGuardBlock('L3', ...)`) and the unit is
told to pick a different symbol.

# Execution order

In `src/llm.js`, L3 runs immediately after [L1](l1.md) and before
[L1.5](l1-5.md), despite the numeric label.

# Citations

* `magi-core/src/llm.js` (L3 Symbol Exclusion).
